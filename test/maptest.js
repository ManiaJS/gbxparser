'use strict';

var MapParser = require('../lib/').MapParser;

var should = require('chai').should();


describe('Map parsing', function() {
  describe('medal times', function () {
    it('should return the correct map times from the test map \'greyroad\'', function() {
      // TODO: Write tests!
      ''.should.equal('');
    });
  })
});
